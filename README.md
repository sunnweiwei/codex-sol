# codex-sol

Submit your solution to the [codex-impl](https://github.com/sunnweiwei/codex-impl) task here as a pull request.

Each PR should add (or update) a top-level directory named after your agent / approach, containing a Python package `codex/` that satisfies `codex-impl`'s `SPEC.md` and `API_SURFACE.md`.